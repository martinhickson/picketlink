<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.picketlink</groupId>
    <artifactId>picketlink-parent</artifactId>
    <version>3.0.1</version>
    <relativePath>../../../../pom.xml</relativePath>
  </parent>

  <artifactId>angular-auth-ui-it</artifactId>
  <packaging>jar</packaging>
  <name>PicketLink Auth UI WildFly Integration Tests</name>
  <description>Arquillian integration tests for Angular auth UI, OAuth client registration, and JWT-protected REST API on WildFly 36</description>

  <properties>
    <wildfly.version>36.0.1.Final</wildfly.version>
    <wildfly-arquillian.version>5.1.0.Final</wildfly-arquillian.version>
    <jboss.home.auth>${project.build.directory}/wildfly-auth</jboss.home.auth>
    <jboss.home.api>${project.build.directory}/wildfly-api</jboss.home.api>
    <test.http.port>8080</test.http.port>
    <test.auth.host>127.0.0.110</test.auth.host>
    <test.api.host>127.0.0.111</test.api.host>
    <test.auth.base.url>http://${test.auth.host}:${test.http.port}/auth</test.auth.base.url>
    <test.api.base.url>http://${test.api.host}:${test.http.port}/api</test.api.base.url>
    <httpcomponents.version>4.5.14</httpcomponents.version>
    <junit.version>4.13.2</junit.version>
    <shrinkwrap.version>1.2.6</shrinkwrap.version>
    <picketlink.auth.jwt.secret>picketlink-auth-it-secret</picketlink.auth.jwt.secret>
    <picketlink.auth.base.url>${test.auth.base.url}</picketlink.auth.base.url>
    <picketlink.auth.jwt.issuer>${test.auth.base.url}</picketlink.auth.jwt.issuer>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.wildfly.arquillian</groupId>
        <artifactId>wildfly-arquillian-bom</artifactId>
        <version>${wildfly-arquillian.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.wildfly.arquillian</groupId>
        <artifactId>wildfly-arquillian-project-bom</artifactId>
        <version>${wildfly-arquillian.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.picketlink</groupId>
      <artifactId>picketlink-auth</artifactId>
      <version>${project.version}</version>
      <exclusions>
        <exclusion>
          <groupId>org.jboss.logging</groupId>
          <artifactId>jboss-logging</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.jboss.logging</groupId>
      <artifactId>jboss-logging</artifactId>
      <version>3.6.1.Final</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.wildfly.arquillian</groupId>
      <artifactId>wildfly-arquillian-container-managed</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.arquillian.junit</groupId>
      <artifactId>arquillian-junit-container</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.arquillian.protocol</groupId>
      <artifactId>arquillian-protocol-servlet-jakarta</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap</groupId>
      <artifactId>shrinkwrap-api</artifactId>
      <version>${shrinkwrap.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap</groupId>
      <artifactId>shrinkwrap-impl-base</artifactId>
      <version>${shrinkwrap.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>httpclient</artifactId>
      <version>${httpcomponents.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <testResources>
      <testResource>
        <directory>src/test/resources</directory>
        <filtering>true</filtering>
      </testResource>
    </testResources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.6.1</version>
        <executions>
          <execution>
            <id>copy-auth-jar</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>org.picketlink</groupId>
                  <artifactId>picketlink-auth</artifactId>
                  <version>${project.version}</version>
                </artifactItem>
              </artifactItems>
              <outputDirectory>${project.build.directory}/test-libs</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.googlecode.maven-download-plugin</groupId>
        <artifactId>download-maven-plugin</artifactId>
        <version>1.9.0</version>
        <executions>
          <execution>
            <id>download-wildfly</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>wget</goal>
            </goals>
            <configuration>
              <url>https://github.com/wildfly/wildfly/releases/download/${wildfly.version}/wildfly-${wildfly.version}.zip</url>
              <unpack>true</unpack>
              <outputDirectory>${project.build.directory}</outputDirectory>
              <skipCache>false</skipCache>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>stage-wildfly-instances</id>
            <phase>process-test-resources</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <delete dir="${jboss.home.auth}" quiet="true"/>
                <delete dir="${jboss.home.api}" quiet="true"/>
                <copy todir="${jboss.home.auth}">
                  <fileset dir="${project.build.directory}/wildfly-${wildfly.version}"/>
                </copy>
                <copy todir="${jboss.home.api}">
                  <fileset dir="${project.build.directory}/wildfly-${wildfly.version}"/>
                </copy>
                <chmod dir="${jboss.home.auth}/bin" perm="ugo+rx" includes="*.sh"/>
                <chmod dir="${jboss.home.api}/bin" perm="ugo+rx" includes="*.sh"/>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>install-loopback-addresses</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>java</goal>
            </goals>
            <configuration>
              <mainClass>org.picketlink.auth.it.support.LoopbackAddressInstaller</mainClass>
              <classpathScope>test</classpathScope>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <skipTests>true</skipTests>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <includes>
            <include>**/*IT.java</include>
          </includes>
          <systemPropertyVariables>
            <jboss.home.auth>${jboss.home.auth}</jboss.home.auth>
            <jboss.home.api>${jboss.home.api}</jboss.home.api>
            <test.http.port>${test.http.port}</test.http.port>
            <test.auth.host>${test.auth.host}</test.auth.host>
            <test.api.host>${test.api.host}</test.api.host>
            <test.auth.base.url>${test.auth.base.url}</test.auth.base.url>
            <test.api.base.url>${test.api.base.url}</test.api.base.url>
            <picketlink.auth.base.url>${picketlink.auth.base.url}</picketlink.auth.base.url>
            <picketlink.auth.jwt.issuer>${picketlink.auth.jwt.issuer}</picketlink.auth.jwt.issuer>
            <picketlink.auth.jwt.secret>${picketlink.auth.jwt.secret}</picketlink.auth.jwt.secret>
          </systemPropertyVariables>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
