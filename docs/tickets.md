# Tickets — known gaps

| # | Ticket | Status |
|---|---|---|
| PL-101 | Oracle dialect verification deferred — add gated Oracle contract test mirroring the PostgreSQL one so verification runs automatically once `PICKETLINK_TEST_ORACLE_*` env vars exist | Fixed (test gated on env) |
| PL-102 | Admin UI element verified only manually + ad-hoc jsdom smoke — wire the smoke test into the Maven build so CI enforces host-router isolation | Fixed |
| PL-103 | OIDC RP-initiated logout not implemented | Fixed |
| PL-104 | OIDC `request`/`request_uri` authorization parameters silently ignored — must be rejected explicitly per spec (`request_not_supported`) | Fixed |
| PL-105 | Refresh token store is in-memory per server instance — revocation/rotation state lost on restart; add JDBC-backed store (dialect-aware, hashed values) | Fixed |
| PL-106 | Real-browser (GUI) automation of the admin element | Fixed — `npm run test:browser` drives system Chromium headless against the local demo stack (`npm run demo`, fixture API, no external network): 17 checks covering all four screens rendering live data, secret masking, active-key marking, hashed-token display and host-router isolation; screenshots to `/tmp`. jsdom suite (14 checks) remains the Maven-build gate. Also surfaced and fixed two real element bugs: `$localize` crash (i18n attributes without `@angular/localize`) and missing `AdminApiService` provider |
| PL-107 | OIDC provider servlets had no servlet-context bootstrap — deployment required manual assembly | Fixed (`OidcProviderServletContextListener` + `modules/oidc/PROVIDER.md`) |
| PL-108 | OIDC provider login not wired to PicketLink IDM (the project's own identity store) | Fixed — `IdmSubjectAuthenticator` verifies passwords through IDM's credential pipeline and derives standard OIDC profile claims (`email`, `name`, `given_name`, `family_name`, `preferred_username`) from the IDM user via the new `ClaimSource` SPI; ID tokens and UserInfo carry them |
| PL-109 | IDM JSON-CLOB document store (`JdbcClobIdmDocumentStore`) violated the repository DB rules: H2 test dependency and `jdbc:h2:` URLs, hardcoded Oracle-style `CLOB` column (not a SQLite type) and `TIMESTAMP` columns (unparseable when read back on SQLite) | Fixed — dialect layer moved to `picketlink-common` (`org.picketlink.common.db.SqlDialect(s)`) and shared by the auth and IDM stores; IDM store DDL is dialect-aware (TEXT on SQLite/PostgreSQL, CLOB on Oracle) with epoch-millis `updated_at`; H2 dependency removed, tests run on SQLite (61/61), gated PostgreSQL contract test added for parity. Note: `modules/rest` (dormant, outside the reactor) still carries H2 references in its pom/test persistence.xml — migrate on revival |
| PL-110 | REST identity services for corporate apps (users/groups/roles provisioning that OIDC and SAML can authenticate against) — must follow the existing REST spec for this, not a bespoke API: the repo already ships SCIM 1.1 in `modules/rest` | Fixed — `ScimServlet` (SCIM 2.0 / RFC 7643-7644, the spec corporate IdPs and provisioning tools speak) in `picketlink-idm-admin`: `/ServiceProviderConfig`, `/Users`, `/Groups` with RFC list-response envelopes, `userName`/`displayName eq` filters, passwords via the SCIM passwords attribute through IDM's credential pipeline, group membership with PUT-replace semantics, RFC error responses, `IdentityRestAccess` bearer guard (static-token init-param or custom). SCIM-provisioned users authenticate immediately through the OIDC `IdmSubjectAuthenticator` over the same partition manager (test-verified). The old SCIM 1.1 module stays as reference; retirement tracked separately if desired |
| PL-111 | JWT layer gaps vs a 10/10 rating: no Ed25519, no clock-skew handling, verbose assembly, no resource-server validator builder, and no client-side token handling (the biggest developer time sink) | Fixed — `PicketLinkJwt` fluent facade (issuer→algorithm→build; issue/validate/revoke/jwks/rotate in five lines, sane defaults: 15 min tokens, 1 h cap, single-algorithm allow-list, 30 s skew); Ed25519/EdDSA end-to-end (JCA signing/verification, OKP JWK publication per RFC 8037, rejected by other algorithms' allow-lists — confusion-safe); ES256 fixed to the proper ECDSA JOSE provider (DER→raw conversion); skew-aware validation threaded through `JwtSigningService`/`JwtIssuanceManager`; `JwtRequirements` audience/scope/subject builder; `TokenClient` m2m token endpoint client (Basic auth, caching, refresh 30 s ahead of expiry, invalidate-on-401, thread-safe, JDK-only); `modules/auth/JWT.md` quickstart. 12 new tests (facade 8 + client 4, incl. deterministic clock-drift test); auth module 99/99 |
| PL-112 | OIDC provider interop gaps: ID tokens lacked `at_hash`/`auth_time` (verified by most RP libraries), `request` objects rejected outright, no back-channel logout | Fixed — ID tokens carry `at_hash` (OIDC Core 3.1.3.6: base64url left half of SHA-256 of the access token) and `auth_time` (captured at authentication, carried through the authorization code); signed request objects (`request` JWT param) verified against the client's registered JWKS with iss/sub = client and aud = issuer guarantees (RFC 7523 semantics), parameters overlaid on query params per OIDC Core 6.1 — unsigned request objects and `request_uri` (SSRF-safe) rejected; Back-Channel Logout 1.0: registered clients with `backchannelLogoutUrl` receive a signed logout_token (events claim, no nonce) POSTed on logout, best-effort. 5 new tests (at_hash computation, request-object precedence incl. nonce-only-in-JWT, unsigned/tampered rejection, back-channel POST with verifiable logout_token); oidc module 23/23 |
| PL-113 | OIDC provider lacked proof-of-possession (DPoP), CORS for browser public clients, and spec-correct prompt=none | Fixed — DPoP (RFC 9449) end-to-end: `DpopProofValidator` (typ dpop+jwt, asymmetric-only, embedded jwk, htm/htu binding with query-stripped URI normalization, 30s freshness, single-use jti, RFC 7638 thumbprints) binds access tokens via `cnf.jkt`; DPoP-bound tokens require a fresh matching proof at UserInfo (bound-token-without-proof and wrong-key both 401, test-verified); `ProviderCorsFilter` (origin allow-list, credentials, DPoP header, preflights) for SPAs; `prompt=none` redirects with `error=login_required` per OIDC Core 3.1.2.1. 6 new tests; oidc 29/29 |
